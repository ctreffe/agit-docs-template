# Presentation Sources

Use this directory for maintained presentation source such as CSS, SCSS,
templates or theme configuration when a derived project needs it. Binary
documentation material belongs in `materials/versioned/`; generated format
outputs belong below `output/`.
