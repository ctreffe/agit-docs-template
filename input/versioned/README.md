# Versioned Input Files

This folder contains external documentation files deliberately approved for
Git and assistant access. Accepted textual content may move into `docs/`;
reviewed binary documentation material created or transformed by the project
belongs in `materials/versioned/`. Git versioning does not authorize
publication.
